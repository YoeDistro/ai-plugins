{
  "name": "yocto-mcp-server",
  "version": "1.0.0",
  "description": "Claude MCP server for Yocto development tools and workflows",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "yocto-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build",
    "test": "npm run build && node dist/test.js"
  },
  "keywords": [
    "yocto",
    "bitbake",
    "mcp",
    "claude",
    "ai"
  ],
  "author": "YoeDistro",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
